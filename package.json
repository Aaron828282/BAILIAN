{
  "name": "bailian-relay-workbench",
  "version": "1.1.0",
  "private": true,
  "description": "独立百炼 Key 池中转工作台",
  "engines": {
    "node": ">=24.0.0 <25"
  },
  "scripts": {
    "init": "node scripts/init.cjs",
    "start": "node src/main.cjs",
    "local": "node --env-file=.env src/main.cjs",
    "test": "node --test test/*.test.cjs",
    "check": "node scripts/check.cjs",
    "release": "node scripts/release.cjs"
  }
}
